# Whale-Well-Whale
This repository compares two deep learning approaches for North Atlantic Right Whale call detection, a ResNet18 model trained on log mel spectrograms and a BEATs based audio model, while also examining how preprocessing choices, such as filtering and feature extraction, affect each model’s performance.
